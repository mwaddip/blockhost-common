#!/bin/bash
set -e

if [ "$1" = "remove" ] || [ "$1" = "upgrade" ]; then
    systemctl stop blockhost-root-agent 2>/dev/null || true
    systemctl disable blockhost-root-agent 2>/dev/null || true
fi

#DEBHELPER#

exit 0
